# =============================================================================
# rm-sync environment configuration
# Copy to .env and fill in real values. `.env` is gitignored.
# Run `make setup` to bootstrap this file with a generated JWT secret.
# =============================================================================

# --- Network / NAS ---
# IP of NAS hosting the NFS exports under /Docker/rm-sync/
NAS_IP=192.168.0.134

# --- Domain ---
# Public hostname for rmfakecloud. Must resolve to this server's public IP
# and have port 443 forwarded at the router.
RMFAKECLOUD_DOMAIN=remarkable.BrothersBrothers.net

# --- Cloudflare (for DNS-01 ACME challenge) ---
# Create at https://dash.cloudflare.com/profile/api-tokens with permissions:
#   Zone -> DNS -> Edit
#   Zone -> Zone -> Read
# Scoped to the zone serving RMFAKECLOUD_DOMAIN.
CF_API_TOKEN=

# --- Let's Encrypt ---
# Email for expiry notices and ToS.
ACME_EMAIL=

# Use STAGING while validating setup (browsers will warn; rate limits are generous).
# Switch to production once a staging cert issues successfully:
#   ACME_CA=https://acme-v02.api.letsencrypt.org/directory
ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory

# --- rmfakecloud ---
# 32+ char random secret. `make setup` generates one automatically.
JWT_SECRET_KEY=
